from setuptools import find_packages, setup
setup(
    name='raspi-servo',
    packages=find_packages(include=['raspi-servo']),
    version='0.1.0',
    description='Intergation of servo motor with raspberry pi',
    author='ashish',
    license'MIT',
)
